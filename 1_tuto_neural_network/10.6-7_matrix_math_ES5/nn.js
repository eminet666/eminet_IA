// numI = number of Inputs
// mumH = number of Hidden
// numO = number of Outputs

function NeuralNetwork(numI,numH, numO) {
    this.input_nodes = numI;
    this.hidden_nodes = numH;
    this.output_nodes = numO;

}